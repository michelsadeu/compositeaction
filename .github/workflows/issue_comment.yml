name: CI Comment Parser

on:
  issue_comment:
    types: [created]

permissions:
  contents: read
  issues: write
  pull-requests: write
  statuses: write

concurrency:
  group: ci-comment-${{ github.event.issue.number }}
  cancel-in-progress: false

jobs:
  parse_ci_comment:
    # Ne s'exécute QUE sur les PR (pas les issues) et pas sur main
    if: |
      github.event.issue.pull_request != null &&
      startsWith(github.event.comment.body, '/ci') &&
      github.ref != 'refs/heads/main'
    runs-on: ubuntu-latest
    environment: GH_APP_TOKEN
    name: Parse /ci command from pull request comment
    
    steps:
      - name: Checkout main branch (for action composite)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Fetch and checkout PR branch
        run: |
          git fetch origin pull/${{ github.event.issue.number }}/head:pr-branch
          git checkout pr-branch
      
      - name: Parse /ci parameters
        id: parse
        uses: ./ci-action
        with:
          github-token: ${{ secrets.GH_API_TOKEN }}
      
      - name: Display parsed parameters
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📊 CI Command Parameters Parsed"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🎯 Target:   ${{ steps.parse.outputs.target }}"
          echo "📦 Version:  ${{ steps.parse.outputs.version }}"
          echo "📈 Coverage: ${{ steps.parse.outputs.coverage }}"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          echo "📋 All checks parameters:"
          echo "${{ steps.parse.outputs.parameters }}"
      
      - name: Post success comment
        if: success()
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GH_API_TOKEN }}
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '✅ CI command parsed successfully!\n\n```json\n${{ steps.parse.outputs.parameters }}\n```'
            });
      
      - name: Post failure comment
        if: failure()
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GH_API_TOKEN }}
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '❌ Failed to parse CI command. Please check the format: `/ci target:xxx; version:yyy; coverage:zzz`'
            });
