name: CI Comment Parser

on:
  issue_comment:
    types: [created, edited]

permissions:
  contents: read
  issues: write
  pull-requests: write
  statuses: write

jobs:
  parse_ci_comment:
    # Ne s'exécute que si le commentaire commence par /ci
    if: startsWith(github.event.comment.body, '/ci')
    runs-on: ubuntu-latest
    name: Parse /ci command from issue comment
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      
      - name: Checkout Pull Request
        if: github.event.issue.pull_request != null
        env:
          GITHUB_TOKEN: ${{ secrets.GH_API_TOKEN }}
        run: |
          PR_NUMBER=${{ github.event.issue.number }}
          echo "Checking out PR #$PR_NUMBER"
          gh pr checkout $PR_NUMBER
      
      - name: Parse /ci parameters
        id: parse
        uses: ./ci-action
        with:
          github-token: ${{ secrets.GH_API_TOKEN }}
      
      - name: Display parsed parameters
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📊 CI Command Parameters Parsed"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🎯 Target:   ${{ steps.parse.outputs.target }}"
          echo "📦 Version:  ${{ steps.parse.outputs.version }}"
          echo "📈 Coverage: ${{ steps.parse.outputs.coverage }}"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          echo "📋 All actions check parameters:"
          echo "${{ steps.parse.outputs.parameters }}"
      
      - name: Post success comment
        if: success()
        uses: actions/github-script@v7
        with:
          github-token: ${{ github.token }}
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '✅ CI command parsed successfully!\n\n```json\n${{ steps.parse.outputs.parameters }}\n```'
            });
      
      - name: Post failure comment
        if: failure()
        uses: actions/github-script@v7
        with:
          github-token: ${{ github.token }}
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '❌ Failed to parse CI command. Please check the format: `/ci target:xxx; version:yyy; coverage:zzz`'
            });
